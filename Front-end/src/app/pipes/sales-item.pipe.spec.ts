import { SalesItemPipe } from './sales-item.pipe';

describe('SalesItemPipe', () => {
  it('create an instance', () => {
    const pipe = new SalesItemPipe();
    expect(pipe).toBeTruthy();
  });
});
