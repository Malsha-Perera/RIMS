

export class Globals{
 /*   
  static list :Array<String>=[];

  static func(bookCode:String): Array<String> {       
    this.list.push(bookCode);
    console.log(this.list);
    return this.list;       
  }
  

  static remove(array, element) {
    const index = array.indexOf(element);
    array.splice(index, 1);
  }

  static funcReturn(bookCode:String): Array<String> {     
    this.remove(this.list, bookCode);      
    console.log(this.list);
    return this.list;  
    
  }

  static getList(){
    return this.list;
  }

  static funcGet(bookCode:String, copy:Number){
    var count = 0;
    for (var i=0; i<this.list.length; i++){
      if (this.list[i]===bookCode){
        count++;
      }
    }
    
    if (count===copy){
      console.log(bookCode+" has "+copy);
      return true;
    }
    else{
      
      return false;
    }
   
  }
     
    */
   static recipeCode = '';
   static productName= '';
   static profitMargin = 20;
  
  
}